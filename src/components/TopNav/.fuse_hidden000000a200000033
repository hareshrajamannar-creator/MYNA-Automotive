// TopNav is retired — the global TopBar in App.tsx now handles the module title and
// app-level actions (Plus, Ask BirdGPT, hamburger). This file is kept so all existing
// screen imports continue to compile without changes.
export function TopNav(_props: Record<string, unknown>) {
  return null
}
